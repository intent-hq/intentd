[package]
name = "intent-context"
version = "0.8.20"
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
license.workspace = true
description = "ContextEngine implementation (AuggieContextEngine) + discovery"

[dependencies]
intent-core = { path = "../intent-core", version = "0.8.20" }
async-trait = "0.1"
tokio = { workspace = true }
directories = { workspace = true }

[target."cfg(unix)".dependencies]
nix = { workspace = true, features = ["signal", "process"] }

[dev-dependencies]
ctor = "1.0.10"
tempfile = "3"
tokio = { workspace = true }

[lints]
workspace = true
