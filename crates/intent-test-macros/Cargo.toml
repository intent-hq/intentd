[package]
name = "intent-test-macros"
version = "0.9.55"
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
license.workspace = true
description = "Test-only attribute macros: run a tokio test with a bound Caller (never a production dependency)"
publish = false

[lib]
proc-macro = true

[dependencies]

[dev-dependencies]
intent-core = { path = "../intent-core" }
tokio = { workspace = true }
