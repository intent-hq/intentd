[package]
name = "intent-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
description = "Intent domain vocabulary: ids, errors, config, events, and cross-layer traits (leaf crate)"

[dependencies]
thiserror = { workspace = true }
