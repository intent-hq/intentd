[package]
name = "intent-store"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
description = "SQLite + file-tree persistence (repositories, migrations, locking)"

[dependencies]
intent-core = { path = "../intent-core" }
