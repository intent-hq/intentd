[Unit]
Description=Intent backend daemon (intentd)
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/intentd serve
ExecStop=/usr/bin/intentd stop
Restart=on-failure

[Install]
WantedBy=default.target
